
init_config:
  loader: core
  use_device_id_as_hostname: true
  profiles:
    my-profile:
      definition_file: test_profile.yaml
    


instances:

- ip_address: 127.0.0.1
  snmp_version: 2
  profile: my-profile
    #loader: core
  community_string: ipsec
  use_device_id_as_hostname: true

    
    

    